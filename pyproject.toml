[project]
name = "llm-as-middle"
version = "0.2.0"
description = "Intelligent MCP Agent for Confluence with LangGraph workflow"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp",
    "openai",
    "httpx",
    "python-dotenv",
    "uvicorn",
    "starlette",
    "langgraph",
    "langchain",
    "langchain-openai",
    "boto3",
    "pydantic"
]

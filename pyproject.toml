[project]
name = "llm-as-middle"
version = "0.4.0"
description = "Intelligent MCP Agent for Confluence and LeanIX with LangGraph workflow and MSAL authentication"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp",
    "openai",
    "httpx",
    "python-dotenv",
    "uvicorn",
    "starlette",
    "langgraph",
    "langchain",
    "langchain-openai",
    "boto3",
    "pydantic",
    "msal"
]
